# Default env: what `npm run build` (the GitHub Pages production build) uses. Rich Mirror (ticket 44) stays off
# here until it's trusted -- see ADR 0006's amendment note and src/featureFlags.ts.
VITE_RICH_MIRROR=false
