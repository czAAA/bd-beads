# Default env: what every build (including `npm run build`, the GitHub Pages production build) uses. Rich Mirror
# (ticket 44) is on everywhere now that it's shipped -- see ADR 0006's amendment note and src/featureFlags.ts.
VITE_RICH_MIRROR=true
