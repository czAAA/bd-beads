# 20: Split the top bar into two boxes

**What to build:** Split the top bar into two separate boxes: a dark-ink-background box holding only the "bd-beads" logo/title, and an aqua-island-background box holding the current-pattern summary + language toggle — replacing today's single combined aqua bar.

**Blocked by:** None (can start immediately)

**Status:** ready-for-agent

- [ ] Top bar renders as two visually distinct bordered boxes instead of one
- [ ] Left box contains only the "bd-beads" title; background uses the existing ink/cello dark color
- [ ] Right box contains the current-pattern summary + language toggle; background stays aqua-island (unchanged from today)
- [ ] No new color tokens introduced — both boxes reuse existing tokens
- [ ] Border/spacing style matches the app's existing box conventions
